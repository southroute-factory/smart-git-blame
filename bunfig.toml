# Bun configuration
# https://bun.sh/docs/runtime/bunfig

[test]
# Set root to src/ to exclude e2e/ and test-fixtures/ directories
# Playwright E2E tests must be run with: npm run test:e2e
root = "src"

# Preload files before running tests (if needed)
# preload = ["./test-setup.ts"]
